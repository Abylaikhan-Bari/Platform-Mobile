plugins {
    id "com.android.application"
    id "kotlin-android"
    id "com.google.gms.google-services" // For Firebase integration
    id "dev.flutter.flutter-gradle-plugin" // Flutter plugin
}

android {
    namespace = "com.aikei.platform"
    compileSdk = 34

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"
    }

    defaultConfig {
        applicationId = "com.aikei.platform"
        minSdk = 23  // Changed from 21 to 23 as required by firebase_auth
        targetSdk = 34
        versionCode = 1
        versionName = "1.0"
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.." // Flutter source location
}

// Apply the Firebase plugin
apply plugin: "com.google.gms.google-services"
